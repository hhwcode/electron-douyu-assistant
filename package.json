{
  "name": "electron-demo",
  "version": "1.0.0",
  "description": "electron",
  "main": "./main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "package": "electron-packager ./ douyu-assistant --platform=win32 --arch=ia32 --out ./outApp --version 1.8.4"
  },
  "author": "hehongwei",
  "license": "MIT",
  "dependencies": {
    "jquery": "^3.3.1",
    "request": "^2.85.0"
  },
  "devDependencies": {
    "electron-packager": "^12.0.1",
    "electron": "^1.8.4"
  }
}
